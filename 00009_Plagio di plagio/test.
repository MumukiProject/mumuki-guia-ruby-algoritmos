it "componer debe crear una nueva cancion a partir de dos canciones"  do
  expect(componer(["las", "tazas", "sobre", "el", "mantel"], ["fue", "unos", "meses", "a", "Caseros"])).to eq ["las", "fue", "tazas", "unos", "sobre", "meses", "el", "a", "mantel", "Caseros"]
end

it "componer debe aparear una lista y luego aplanar el resultado"  do
  expect(componer([1, 2, 3], [4, 5, 6])).to eq [1, 2, 3, 4, 5, 6]
end

